
events {
    multi_accept       on;
}

http {
    server {
        include     gzip.conf;
        include     port.conf;
        server_name example.com;
        root        ../unmined-web/;
        index       unmined.index.html;
    }
}